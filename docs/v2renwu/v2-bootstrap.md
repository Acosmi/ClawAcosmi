# Acosmi V2 重构阶段 - 新窗口通用启动上下文 (Bootstrap)

> **使用说明**：复制以下内容发送到新对话窗口，即可让 AI 快速恢复当前六大板块重构的宏观进度和工作规范，并无缝切入后续的修复阶段。

---

由于上下文长度限制，这是一个新的对话窗口。请你先读取本指南以快速恢复项目的宏观背景。

### **[1. 背景与现状]**

当前项目：**Acosmi 后端重构** (由原版 TS 服务端迁移重写至 Go，致力于剔除黑盒且全量掌控代码树)。
当前进展：**全局深度联调审计阶段 (V2 Audit) 已全部完成**，覆盖 W1 到 W6 的六大核心产品线。
文档索引点：

- **深度审计对照报告** (含详细的文件及隐藏依赖覆盖情况) 位于 `docs/renwu/global-audit-v2-W*.md`。
- **待执行修复跟踪清单** (指引我们接下来的行动) 位于 `docs/v2renwu/v2-w*.md`，主入口为 `docs/v2renwu/v2-index.md`。

### **[2. 各模块健康度速览]**

- **W1 (Gateway / Security / Config)**: 健康状况 **A**，全量对齐，无结构性缺陷。
- **W2 (Agents)**: 健康状况 **C+**，存在工具挂载不全(幽灵化) 及 跨进程锁安全隐患等 P0/P1 阻断项。
- **W3 (Channels)**: 健康状况 **A**，主流通道（LINE/WhatsApp等）全面跑通，历史包袱扫除。
- **W4 (Auto-reply / Cron / Daemon / Hooks)**: 健康状况 **C**，Linux 守护进程生命周期断裂 (Linger) 以及重要指令管线未迁移。
- **W5 (Infra / CLI / Media / Memory)**: 健康状况 ~~C-~~ → **B**，7/7 项全部修复/确认完备，3 项 CLI 子命令推迟至中长期规划。
- **W6 (TUI / Browser)**: TUI 端庄成熟 (A)，Browser 底座为 Stub (C)，亟需接通后端 Playwright 节点动作。

### **[3. 当前的目标行动]**

在接下来的对话中，我将指导你深入某个具体的 `Wn` 板块进行缺陷修复或功能补充。
在此之前，请遵守针对本项目的核心 `@[/refactor]` 策略与纪律：

1. **不要一次性修太多**：一次仅聚焦修复当前 `docs/v2renwu/v2-w*.md` 里清单标明的一个或两个缺陷点，完成后必须在本地跑通 `go build` 等。
2. **严防黑盒依赖**：禁止通过 NPM 等机制引入非 Go Native 或隐秘跨边界的代码。
3. **保持文档同步**：每修复或合入一个模块，请务必更新对应的跟踪清单状态。

请告诉我你已加载上下文，并准备好进入针对某一板块的攻关修复计划！
