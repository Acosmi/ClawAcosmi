---
summary: "打开 Control UI（Web 控制台）"
read_when:
  - You want to open the Control UI with your current token
  - You want to print the URL without launching a browser
title: "dashboard"
status: active
arch: rust-cli
crate: oa-cmd-supporting
---

> [!NOTE]
> **架构状态：✅ Rust CLI 已实现** — 对应 crate `oa-cmd-supporting`（`dashboard` 模块）。
> 入口二进制：`openacosmi`（Rust）。

# `openacosmi dashboard`

Open the Control UI using your current auth.

```bash
openacosmi dashboard
openacosmi dashboard --no-open
```
