---
summary: "CLI reference for `openacosmi sessions` (list stored sessions + usage)"
read_when:
  - You want to list stored sessions and see recent activity
title: "sessions"
---

# `openacosmi sessions`

List stored conversation sessions.

```bash
openacosmi sessions
openacosmi sessions --active 120
openacosmi sessions --json
```
