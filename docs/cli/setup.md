---
summary: "CLI reference for `openacosmi setup` (initialize config + workspace)"
read_when:
  - You’re doing first-run setup without the full onboarding wizard
  - You want to set the default workspace path
title: "setup"
---

# `openacosmi setup`

Initialize `~/.openacosmi/openacosmi.json` and the agent workspace.

Related:

- Getting started: [Getting started](/start/getting-started)
- Wizard: [Onboarding](/start/onboarding)

## Examples

```bash
openacosmi setup
openacosmi setup --workspace ~/.openacosmi/workspace
```

To run the wizard via setup:

```bash
openacosmi setup --wizard
```
