# Argus-compound 待办事项

> 更新日期：2026-02-11

## P3: Ollama 模型管理面板（可选增强）

P3 核心功能（Ollama 作为 VLM Provider）已通过 P1 的 OpenAI 兼容层覆盖。
以下为可选增强功能，按需实现：

- [ ] Ollama 模型管理 API — `GET /api/ollama/models`、`POST /api/ollama/pull`
- [ ] 前端 Ollama 面板 — 显示已安装模型、拉取新模型、删除模型
- [ ] 模型下载进度显示 — SSE 流式推送拉取进度
- [ ] 多 Ollama 实例管理 — 支持配置多个 Ollama 端点

**优先级**：低 — Ollama 自带 Web UI 和 CLI 即可管理模型

---

## P4: C++ 高性能推理层

为边缘推理 / 离线场景预留 CGO C++ 通道。前期 Ollama 满足业务需求，暂缓实施。

**触发条件**（满足任一即启动）：

- 需要进程内 ONNX Runtime 推理（不走 Ollama）
- 需要 OpenCV C++ 高性能图像管线
- 需要 GPU 自定义算子

**待实现步骤**：

- [ ] Step A: 创建 `internal/cppbridge/` — C bridge 函数 + Go wrapper
- [ ] Step B: 集成 ONNX Runtime C++ — 模型加载 + 推理接口
- [ ] Step C: 静态链接打包 — 确保单一二进制部署
- [ ] Step D: OpenCV C++ 图像管线 — 高性能视频帧预处理

**预计工作量**：3+ 次对话

---

## 已完成的清理项 ✅

- [x] 移除 `py-vision/` 全部代码（2026-02-10）
- [x] 移除 `openclaw-skills/` 全部代码（2026-02-10）
- [x] 更新 `README.md` 反映 Go-only 架构（2026-02-10）
- [x] 更新 CI 移除 Python 检查任务（2026-02-10）
- [x] 清理 Go 代码中 py-vision 引用（2026-02-10）
- [ ] 更新 `docker-compose.yml` 移除 py-vision 服务（如不再需要）
